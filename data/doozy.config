# Friendly name of the renderer as seen by clients
# FriendlyName = Doozy

# Overide the unque device name if desired (multiple instances in the network)
# UDN = 356a6e90-8e58-11e2-9e96-0800200c9a66

# Audio output configuration (Alsa|OpenAL|PulseAudio)
# AudioOutput = PulseAudio

# Audio device to use in case of multiple devices (alsa only)
# AudioDevice = default
